# Environment Configuration Template
# Copy this file to .env.local and customize for your environment

# ========================================
# DEVELOPMENT ENVIRONMENT
# ========================================
# Use these settings for local development

# Frontend Configuration
VITE_API_BASE_URL=http://localhost:5000
VITE_PROXY_TARGET=http://localhost:5000
NODE_ENV=development

# Backend Configuration
ASPNETCORE_ENVIRONMENT=Development
ConnectionStrings__DefaultConnection=Data Source=./data/library.db

# CORS Settings (array format - add more as needed)
CORS__AllowedOrigins__0=http://localhost:3000
CORS__AllowedOrigins__1=http://localhost:5173
CORS__AllowedOrigins__2=http://localhost:8080

# JWT Configuration
JWT__SecretKey=your-super-secure-secret-key-min-32-chars
JWT__Issuer=LibraryAPI
JWT__Audience=LibraryApp
JWT__ExpiryInMinutes=60

# Docker Configuration
COMPOSE_PROJECT_NAME=library

# ========================================
# PRODUCTION ENVIRONMENT
# ========================================
# Use these settings for production deployment

# Frontend Configuration (Production)
# VITE_API_BASE_URL=https://yourdomain.com
# VITE_PROXY_TARGET=http://backend:8080
# NODE_ENV=production

# Backend Configuration (Production)
# ASPNETCORE_ENVIRONMENT=Production
# ConnectionStrings__DefaultConnection=Data Source=/app/data/library.db

# CORS Settings (Production)
# CORS__AllowedOrigins__0=https://yourdomain.com
# CORS__AllowedOrigins__1=https://www.yourdomain.com

# JWT Configuration (Production - use strong secrets!)
# JWT__SecretKey=your-production-secret-key-should-be-very-long-and-random
# JWT__Issuer=LibraryAPI
# JWT__Audience=LibraryApp
# JWT__ExpiryInMinutes=60

# Backup Configuration (Production)
# BACKUP_SCHEDULE=0 2 * * *
# DATA_DIR=./data
# BACKUP_DIR=./backups